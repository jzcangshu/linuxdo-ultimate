# 更新日志

本项目遵循 Semantic Versioning（语义化版本）：兼容性修复递增补丁号，兼容性新功能递增次版本号，不兼容变更递增主版本号。

## 未发布

## 0.6.17 - 2026-08-17

- 修复点击帖子列表空白区域时帖子覆盖列表页的问题，现统一在阅读区打开。

## 0.6.16 - 2026-08-16

- 设置面板改为可滚动的右侧侧拉窗口，保留顶栏图标入口。
- 选中文本操作条新增“一键 Base64”，可打开不随页面点击消失的可拖拽编码/解码窗口，并自动填入当前选区。
- 设置“实用工具”新增“一键 Base64”开关，默认开启。
- 设置面板底部操作按钮的文字保持单行显示。

## 0.6.15 - 2026-08-10

- 将内嵌页的初始页面工具配置直接随 iframe（内嵌页面）传递，不再等待完整运行模块回报就绪后才生效。
- 极简模式的公告、列表头像、分类徽章和话题标签在文档启动阶段即应用，避免页面显示后再突然消失。
- “只看楼主”按初始配置提前启动，原有页面消息仍负责后续设置变更，不新增轮询或页面监听。

## 0.6.14 - 2026-08-10

- 帖子 iframe（内嵌帖子页）启动时提前加载“只看楼主”模块，缩短按钮首次出现时间。
- 避免模块尚未加载时点击论坛原生“全部显示”无法清除只看楼主状态的问题。

## 0.6.13 - 2026-08-10

- 垂直标签栏方向改为跟随整体帖子正文位置：左侧布局的所有阅读区统一靠左，右侧布局的所有阅读区统一靠右。
- 同步统一双阅读区的展开方向、标题对齐、活动高亮、滚动条遮盖、固定模式和触控布局，不再按主次阅读区分别判断。

## 0.6.12 - 2026-08-10

- 修复垂直标签栏方向选择器优先级过高，导致鼠标悬停后仍无法展开的问题。
- 左右裁剪方向改由独立样式变量管理，统一恢复单/双阅读区的悬停、键盘聚焦、拖拽锁定、固定展开和触控布局。

## 0.6.11 - 2026-08-10

- 修复右侧布局开启双阅读区后，主阅读区垂直标签栏错误靠右的问题；主阅读区保持左侧交互，最右阅读区保持右侧交互。
- 标签栏标题图标、文字对齐、活动高亮、滚动条遮盖、阴影及触控布局均按各阅读区的实际方向统一处理。
- 移除预览 iframe 的权限限制，允许目标网页完整运行脚本、弹窗和下载；链接悬停仍不发起预热请求。

## 0.6.10 - 2026-08-10

- 精简布局、标签页和性能设置文案，正文位置、会话恢复等名称改为直接描述实际行为。
- 标签页上色移入“标签页样式”的树状子项，并补充分类上色说明；垂直标签栏的两个专属选项保持按样式显隐。
- 父项与子项合并为同一设置组，仅在整组末尾显示分隔线；树状连接线略微增强对比度。
- “只看楼主”移除设置入口并在帖子正文中始终可用；列表页和分屏宿主页仍不加载对应运行模块。
- 设置结构升级到版本 5，旧的只看楼主开关值被安全忽略，其余版本 4 设置完整保留。

## 0.6.9 - 2026-08-10

- 重构设置页信息层级：分类标题字号更醒目，真实依赖项使用淡色树状连接线与缩进，不跨分类建立父子关系。
- “页面样式”改名为“论坛美化”，标签分类上色移入该分类；其余阅读、布局和实用工具保持独立。
- “清爽模式”改名为“极简模式”，列表头像、公告、分类徽章和话题标签可在紧凑子项中分别控制。
- 设置结构升级到版本 4；旧版极简设置无损迁移，页面工具协议只下发实际生效的隐藏项，不增加页面监听。

## 0.6.8 - 2026-08-10

- 修复帖子或列表 iframe（内嵌页面）在内部整页导航后未重新收到当前功能配置的问题。
- 修复原生“显示全部”与“只看楼主”状态的时序冲突，保留论坛原生筛选提示和热门回复逻辑。
- 长帖楼主信息缺失时，仅在用户点击“只看楼主”后按需读取主题元数据。
- 修复分屏模式下顶部聊天入口无法在列表区打开的问题。

## 0.6.7 - 2026-08-10

- 修复论坛切换作者筛选或重新渲染时间轴控件后“只看楼主”按钮消失且无法恢复的问题；仅在原生控件变化时增量重新挂载。
- 点击论坛蓝色筛选提示中的原生“显示全部”按钮时同步关闭本主题的只看楼主状态，不拦截点击，也不修改或替代论坛提示框。
- “只看楼主”改用与“热门回复”一致的论坛按钮结构、整行尺寸和对齐方式，并增加原生风格的用户勾选图标。

## 0.6.6 - 2026-08-10

- 帖子阅读位置改为完全使用论坛原生楼层定位和账号阅读进度，不再重放旧的绝对像素坐标，修复长帖刷新后因虚拟帖子流继续扩展而延迟跳动的问题。
- 删除帖子帧首次加载、标签切换、内存释放重建和跨阅读区转移时的重复滚动恢复及 15 秒重试计时器；仍保存论坛实时更新的楼层网址。
- 帖子滚动不再持续向宿主页上报像素位置，只有楼层网址变化时才同步会话；列表页的独立滚动恢复保持不变。

## 0.6.5 - 2026-08-09

- “只看楼主”改用论坛原生作者筛选加载数据，不再扫描并隐藏帖子节点，修复长帖深层位置持续补载、页面卡顿和关闭按钮延迟熄灭的问题。
- 只看楼主与热门回复统一使用论坛原生筛选参数；切换热门回复或全部显示时保留楼主筛选，不再互相覆盖。
- 楼主用户名从主题预加载元数据结构化读取，主楼未加载时无需额外请求；关闭功能时恢复全部回复，后台标签冻结时只暂停监听并保留状态。
- 删除旧的帖子隐藏标记、增量过滤队列和楼层扫描逻辑，进一步降低长帖内存与主线程开销。

## 0.6.4 - 2026-08-09

- 修复分屏模式下从通知菜单打开帖子后菜单不关闭，以及“查看所有通知”只改变宿主页地址、列表区没有跳转的问题。
- “只看楼主”改为从当前帖子标记或主题预加载元数据稳定识别楼主；从长帖中部恢复且主楼未加载时也能立即生效，并可与热门回复筛选叠加。
- “只看楼主”按钮固定在时间轴的热门回复按钮上方，文案始终保持不变，启用状态使用论坛原生蓝色按钮样式。
- 修复关闭分屏后，保留的帖子标签状态可能让原生帖子页被误判为分屏宿主页、从而隐藏独立页面工具的问题。
- 自动过盾新增 `429 error` 识别，并以浏览器标签页和页面地址为范围阻止短时间重复跳转。
- 保持只看楼主按需加载、后台冻结时暂停和增量处理机制；不增加网络请求，不解析完整主题预加载数据。

## 0.6.3 - 2026-08-09

- 将 iframe（内嵌页面）入口缩减为轻量加载器，只有插件管理的列表页和帖子页才加载完整通信桥，普通子页面不再承担无关解析成本。
- 将“只看楼主”拆为按需运行模块：关闭时不加载实现、不创建页面监听；后台冻结帖子页会暂停监听，恢复时再增量同步。
- 修复“只看楼主”按钮可能反复触发页面变动、SPA（单页应用）切换帖子后状态串线，以及长帖反复全量扫描的问题。
- 清爽模式与原“隐藏列表头像列”合并为单一设置，删除欢迎文案、特定帖子地址和自动折叠侧栏等脆弱适配；旧设置会自动迁移。
- 低端设备优化改为一次设备判断和根节点样式状态，不再依赖页面监听。
- 正式扩展构建启用压缩，并保持预览核心、通信桥和帖子工具的独立按需加载边界。

## 0.6.2 - 2026-08-09

- 移植 Linux.do 小助手的“只看楼主”工具：可在帖子正文中一键切换只看楼主/查看全部，并按主题保存状态。
- 新增清爽模式开关，隐藏全局公告、欢迎提示、分类徽章、标签和列表头像列。
- 新增低端设备性能优化开关，仅在检测为低端设备时降低动画和过渡开销；不移植黑白灰滤镜。
- README 补充 Linux.do 小助手和 Cloudflare 过盾脚本致谢。

## 0.6.1 - 2026-08-09

- 右侧垂直标签栏的当前标签高亮改到标签右边缘，自动收起后仍可直接识别当前页面。
- 右侧标签栏标题的管理图标移到文字右侧并水平镜像，折叠状态下保持可见；左侧标签栏维持原有方向。

## 0.6.0 - 2026-08-09

- 移植 `@pipecraft` 的 Linux.do Cloudflare 5 秒盾自动跳转逻辑：识别指定 403/响应失效弹窗后进入 Challenge 页面，完成后自动返回原地址。
- 将过盾逻辑构建为独立轻量模块，并在宿主页、列表 iframe 和帖子 iframe 中分别运行；分屏状态下只刷新发生错误的页面区域，不清空其他已加载页面。
- 保留同源回跳校验与 5 秒循环保护，并按分屏 frame 区分保护状态，避免多个阅读区互相阻断恢复。
- Challenge 页面不再启动分屏宿主、页面通信桥或预览功能；兼容用户脚本继续提供原脚本的手动触发菜单。
- README 增加自动过盾说明，并致谢原脚本作者与功能介绍帖。

## 0.5.3 - 2026-08-09

- 修复鼠标点击垂直标签后残留焦点、移出标签栏仍无法自动收起的问题；键盘操作仍保留焦点展开行为。
- 右侧垂直标签栏改为覆盖在正文 iframe 右边缘和原生滚动条上方，不再为收起栏预留独立网格列。
- 单阅读区和双阅读区均使用相同的右侧覆盖逻辑；关闭自动收起时仍保留完整标签栏占位。
- 垂直标签栏的纯展示设置不再重建分屏或重新激活阅读页，切换自动收起时不会刷新 iframe，也不会触发旧阅读位置恢复。
- 用户开始滚动阅读后立即取消尚未完成的旧位置恢复，避免延迟恢复覆盖当前阅读位置。
- 放宽垂直标签栏的行高、间距和内边距，标签标题改为跟随论坛正文级字号；标签管理入口换用箭头列表图标。
- 调整设置文案的信息密度：需要上下文的功能保留明确说明，一眼可懂的项目移除重复灰字；更新日志弹层扩大为带版本、日期和分项说明的阅读卡片，设置标题显示当前版本号，并放大垂直标签栏的分区标题。

## 0.5.2 - 2026-08-08

- 修复关闭官方侧栏后，浏览器最左侧边缘无法触发主阅读区垂直标签栏的问题。
- 修正右侧垂直标签栏的锚点和裁切方向，使其从右向左覆盖展开、从左向右收起，不再向窗口外溢出。
- 隐藏右侧垂直标签列表的原生滚动条，保留滚轮和触控板滚动能力。

## 0.5.1 - 2026-08-08

- 垂直标签栏根据阅读区位置自动贴靠外侧：中间阅读区靠左，右侧阅读区靠右，并同步调整标题对齐。
- 增加阅读区边缘热区，鼠标滑到实际边界即可触发展开，不必精准悬停在收起栏上。
- 修复点击标签后焦点残留导致垂直标签栏离开后无法自动收起的问题。

## 0.5.0 - 2026-08-08

- 新增可选垂直帖子标签栏；每个阅读区拥有独立侧栏，兼容详情页在右、详情页在中间及双阅读区布局。
- 垂直标签栏可自动收起为紧凑图标栏，悬停、键盘聚焦、拖拽或打开右键菜单时覆盖展开，不改变正文 iframe 尺寸和滚动位置。
- 新增可选的主分类自动分组，仅使用内置分类常量表，不扫描页面、不请求网络；分组模式下拖拽仍只提交同分类内的最终顺序。
- 横向和垂直标签统一支持鼠标中键关闭，并共用原有右键菜单、分类上色、活动状态、拖拽排序和节点复用机制。
- 设置面板新增同款分段式标签栏样式切换，并在较矮视口中自适应压缩间距而不引入内部滚动。

## 0.4.2 - 2026-08-08

- 设置面板底部新增“检查更新”按钮，通过仓库内 `updates/latest.json` 获取结构化版本和更新摘要。
- 自动检查延迟到页面稳定后执行，成功结果缓存 24 小时，失败后 1 小时内不自动重试；手动检查始终绕过缓存。
- 检测到新版本时，顶部设置入口与检查更新按钮同时显示与 Ultimate 品牌色一致的亮黄色呼吸提醒。
- 更新检查不自动下载安装，不依赖 GitHub API，也不会在无更新或网络失败时打扰用户。

## 0.4.1 - 2026-08-08

- 标签分类颜色只使用内置固定色表，不再读取列表徽标样式、帖子 Open Graph（开放图谱）颜色元数据、iframe 消息或历史会话中的动态颜色。
- 移除分类颜色解析、跨页面传递和持久化链路，减少帖子与列表通信桥的观察和消息负担。
- 产品和扩展名称统一为 Linux Do Ultimate，设置标题中的 Ultimate 使用亮黄色强调，扩展图标改用 Linux Do 官方圆形 Logo。
- 新增 Chrome 与 Firefox 独立清单、构建目录和正式发布压缩包；两者复用同一套业务源码。

## 0.4.0 - 2026-08-08

- 正式迁移为 Chrome Manifest V3 浏览器插件，完整复用 V0.2.18 的布局、标签、会话、滚动恢复、阅读计数、预览和 LDC 功能实现。
- 将顶层 host、iframe bridge、预览运行时和跨域请求后台拆成四个独立产物；帖子与列表 iframe 不再解析完整宿主应用。
- 预览核心改为结构化延迟加载：默认关闭时不下载、不解析，首次开启或首次处理预览请求时才加载独立运行时。
- 保留原用户脚本构建作为兼容和回归基线；插件与用户脚本不得同时启用。

## 0.2.18 - 2026-08-08

- 顶部导航高度改为读取实际渲染尺寸，并在导航尺寸变化时自动同步，避免固定高度造成分屏错位。
- 修正需求审计文档中过期的多标签可见性描述。
- 合并 frame bridge（页面通信桥）中完全等价的双击延迟常量，保持两类页面行为不变。

## 0.2.17 - 2026-08-08

- 修复大型帖子刷新后阅读位置恢复过早超时的问题：帖子内容仍在增量展开时继续尝试恢复深层滚动位置；切回已存在的阅读标签时也会重新应用保存位置。

## 0.2.16 - 2026-08-08

- 修复标签拖拽结束后中途避让的标签仍残留横向位移、导致标签栏出现空槽的问题；放下、取消或重新渲染时均会完整复位拖拽视觉状态。

## 0.2.15 - 2026-08-08

- 删除项目内不可达的旧版预览实现及其测试，移除对应无效样式；保留并继续使用上游 Link Hover Previewer 4.13.1 核心。

## 0.2.14 - 2026-08-08

- 修复列表导航重试耗尽后永久卡死的问题：刷新或 URL 变化时重置重试计数，确保分屏能够恢复挂载。
- 修复书签整篇主题失败时无任何反馈的问题：缺少主题 ID 或 CSRF token 时返回失败消息并显示提示。
- 修复 LDC 收入组件在找不到默认宿主时静默消失的问题：使用 `.d-header-icons` 末尾作为兜底挂载点。
- 修复 `frame-ready` 消息把已保存的滚动位置临时覆盖为 0 的问题：新载入的 iframe 不参与滚动位置写入，避免状态推理脆弱。
- 文档修正：预览核心版本更新为 4.13.1，设置项说明补充 `maxOpenTabs` 并明确总开关由用户脚本管理器负责。

## 0.2.13 - 2026-08-08

- 修复超出活动页面上限被回收的标签丢失阅读位置的问题：回收前先读取滚动位置，切回该标签时不再从头开始。

## 0.2.12 - 2026-08-08

- 修复手动拖拽的标签顺序被丢弃的问题：打开新帖子、重新打开已有帖子以及刷新恢复后，都保持用户排好的顺序。
- 标签数量超出上限时仍然移除最久未活跃且非当前活动的标签，其余标签的相对顺序不变。

## 0.2.11 - 2026-08-08

- 统一分屏启动顺序：首次点击、刷新恢复、重新启用分屏及直接帖子转入分屏时，先创建需要显示的帖子 iframe（内嵌页面），再启动列表 iframe（内嵌页面）。
- 两类页面仍在同一执行周期并行加载；不设置永久低请求优先级，避免拖慢用户后续主动操作列表。

## 0.2.10 - 2026-08-08

- 链接预览核心改为按需初始化；功能保持关闭时不再安装其样式、监听器和运行状态，首次开启后只安装一次。
- 帖子标签栏改为按标签编号局部更新，切换、改名、上色和排序时复用已有节点，不再整栏销毁重建。
- 从原生非阅读页首次打开帖子时立即显示阅读区，并暂时保留原列表；列表 iframe（内嵌页面）渲染正文并恢复滚动位置后无感接管，异常情况下最多等待三秒。

## 0.2.9 - 2026-08-07

- 单阅读区和双阅读区分别记忆侧栏宽度及列表比例，拖拽调整不再互相覆盖。
- 关闭第二阅读区时恢复单阅读区的已保存比例；旧配置首次切换时使用默认 `65:35`。

## 0.2.8 - 2026-08-07

- 未显示但仍保留的帖子 iframe（内嵌页面）现在会进入可恢复的软冻结状态，暂停脚本自身的观察器、延迟任务、媒体与动画。
- 返回标签时直接恢复原 iframe（内嵌页面）和原有浏览状态；超过活动页上限的页面继续使用原有硬挂起机制释放内存。

## 0.2.7 - 2026-08-07

- 宿主页与内嵌页改用独立样式载荷，列表和帖子 iframe（内嵌页面）不再重复注入完整界面样式。
- 标签主分类颜色改为固定映射，不再等待或扫描侧栏；多级分类继续优先使用主分类颜色。

## 0.2.6 - 2026-08-07

- 减少重复激活阅读 iframe（内嵌页面）时的无效属性写入。
- 标签管理增加单标签查询，避免处理单个消息时复制完整标签列表。
- 帖子页只在标题或分类元数据可能变化时重新解析，跳过高频正文变化。

## 0.2.5 - 2026-08-07

- 修复开启链接预览后，站内帖子点击被上游延迟导航再次接管，导致分屏刚出现便跳回原生单页的问题。
- 站内帖子单击和双击现在完全交由分屏路由处理，站外链接继续使用上游预览核心。

## 0.2.4 - 2026-08-07

- 保持上游加载核心不变，同时恢复“正常加载不显示蒙版、仅失败时显示错误”的既有界面行为。

## 0.2.3 - 2026-08-07

- 将链接预览核心从上游 4.12.2 完整更新为 4.13.1，恢复原版请求、缓存、动态页面渲染及页面活跃状态生命周期。
- 删除合并版自行加入的资源地址重写等加载改造，并增加关键加载函数哈希校验，防止后续无意偏离上游实现。
- 悬停链接不再发起网络预热请求；只有用户实际触发预览后才加载目标内容。
- 保留项目设置入口、分屏内链接转发、统一图标、鼠标刷新、拖动和新标签打开后关闭窗口等外层适配。

## 0.2.2 - 2026-08-07

- 标签拖拽时增加跟手拖动图像和相邻标签平滑避让；仅跨越标签中心时更新目标，放下后才保存顺序。
- 标签分类上色改为主分类优先，多级分类不再使用子分类颜色。
- 修复标签右键菜单的两列网格误把操作文字推到右侧，恢复图标后的左对齐排版。

## 0.2.1 - 2026-08-07

- 基于 `v0.2.0` 稳定架构回迁分类元数据上色、标签拖拽、统一图标和带图标的右键菜单。
- 阅读区关空后自动移除占位，剩余阅读区直接扩充并保留已加载页面。
- 浏览器标签页会话完全隔离；刷新只恢复自身，跨访问只领取最后关闭的一份，并排除刷新产生的伪关闭快照。
- 固定单一存储后端，避免故障降级后混读两份状态；定期回收过期会话和阅读计数锁。
- 右键刷新与整篇主题书签改为后台准备，不再切换当前阅读标签。
- 预览 iframe（内嵌页面）取消运行权限限制，统一操作图标，并按时释放过期缓存与未完成请求。
- 合并 LDC 收入的并发请求，页面隐藏或功能关闭后停止无用刷新。

## 0.2.0 - 2026-08-07

- 将非阅读页改为独立 iframe（内嵌页面），与阅读区互不刷新、互不清理缓存。
- 新增第二阅读区和 Chrome（谷歌浏览器）风格标签右键管理菜单。
- 新增整篇主题书签、标签跨区进度继承及刷新后的收敛式滚动恢复。
- 修复直接帖子分类链接、列表区浮层关闭、阅读区高度和多项导航边界问题。
- 完善会话隔离、生命周期释放、文档和自动化测试。

## 0.1.21 - 2026-08-06

- 上一个公开稳定版本。
