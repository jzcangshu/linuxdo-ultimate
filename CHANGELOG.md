# 更新日志

本项目遵循 Semantic Versioning（语义化版本）：兼容性修复递增补丁号，兼容性新功能递增次版本号，不兼容变更递增主版本号。

## 0.3.2 - 2026-08-07

- 统一内嵌页面启动路径：完整脚本只在顶层解析，父页安装一次轻量桥；列表和阅读页在角色样式与配置就绪前保持隐藏，消除重复就绪、侧栏闪现和延迟跳动。
- 导航消息恢复真实来源与主题身份校验，并加入确认和原生回退；修复列表点击偶发无响应、旧页面迟到消息污染标签、主题重定向写坏状态及顶部私聊入口不可见。
- 标签显示顺序与最近使用时间彻底分离，拖拽顺序可跨刷新保存；跨阅读区移动沿用已加载页面，不再令书签等命令永久排队。
- 多浏览器标签页继续使用独立会话；刷新会撤销自身的跨访问恢复快照，开启恢复时只领取最后关闭的一页，不合并窗口状态；同一浏览器标签页暂时离站再返回仍保留自身状态。
- 列表和阅读位置只在真实就绪后恢复一次，不再定时强推页面底部，避免重新触发无限增量加载。
- 预览移除全部沙箱限制，补齐响应完成后的体积检查和缓存预算执行；同源论坛页面留给分屏导航，附件和下载保持原生。
- LDC 收入请求支持关闭时中止，并使用浏览器原生跨页面锁合并并发刷新；设置存储在单次运行中固定使用同一后端，避免混读新旧状态。
- 扩充分屏启动、消息确认、标签转移、顺序恢复、现代分类颜色、路由、预览和请求生命周期回归测试。

## 0.3.1 - 2026-08-07

- 分屏刷新恢复改为页面主体出现后立即启动，三个内容框架在同一轮初始化中并发创建，并在外壳就绪前遮蔽原生单列内容。
- 脚本创建的列表和阅读框架在文档开始解析时安装轻量桥接与角色样式，避免加载标志、侧栏和原始正文宽度短暂闪现后再次重排。
- 修复用户脚本隔离环境改变消息来源对象后，列表和阅读区点击消息被错误丢弃的问题；消息继续校验同源域与受管理的角色编号。
- 新增首屏并发创建、框架早期识别、跨隔离环境消息及防闪烁样式回归测试。

## 0.3.0 - 2026-08-07

- 用户脚本改为仅在顶层运行，由父页面为列表和帖子内嵌页安装轻量桥接，显著减少重复解析和样式注入。
- 两个阅读区共享活动页面额度；帖子标签总数不再受 50 个硬限制，会话与阅读计数锁加入过期回收和异常降级。
- 预览改为按需安装并可完整卸载，补齐最终网址、大响应、下载和二进制防护，后台预览标签释放运行环境，同时保留完整脚本、同源和表单权限。
- LDC 收入改为可见页轮询、单次请求复用和短时共享缓存，修复并发覆盖、混算、异常加载态及跨域 CSRF 请求头。
- 设置面板增加低高度双列布局，修复重复写入、顶栏高度、无侧栏拖动、标签自适应、菜单键盘操作和严格路由识别。
- 新增主、第二阅读区独立的标签拖拽排序；拖动过程不写会话，放下时只持久化一次。
- 建立统一线性图标系统，覆盖标签、阅读区、设置、预览工具栏及右键菜单全部操作入口。
- 预览缓存按最近到期项自动释放，LDC 失效缓存立即移除，长期页面低频回收过期会话；卸载时取消相应定时器。
- 清理废弃实现与无效测试，区分 DOM 集成测试和真实浏览器验收，启用压缩构建并同步维护文档。

## 0.2.0 - 2026-08-07

- 将非阅读页改为独立 iframe（内嵌页面），与阅读区互不刷新、互不清理缓存。
- 新增第二阅读区和 Chrome（谷歌浏览器）风格标签右键管理菜单。
- 新增整篇主题书签、标签跨区进度继承及刷新后的收敛式滚动恢复。
- 修复直接帖子分类链接、列表区浮层关闭、阅读区高度和多项导航边界问题。
- 完善会话隔离、生命周期释放、文档和自动化测试。

## 0.1.21 - 2026-08-06

- 上一个公开稳定版本。
